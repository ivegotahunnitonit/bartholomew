# Smithery.ai MCP Registry Configuration for Bartholomew (BTP v5.4.12)
# https://smithery.ai/docs/config#smitheryyaml

version: 1.0.0
name: "bartholomew"
displayName: "Bartholomew AI Security Guard & SOC 2 Auditor"
description: "Sub-35µs in-process AST security gate, eBPF kernel sandbox, secret scrubber, and L402 Lightning settlement for AI agent swarms (Claude 3.7, Gemini 3.8, Cursor, AutoGen, Cloudflare)."
homepage: "https://bartholomew.info"
repository: "https://github.com/ivegotahunnitonit/bartholomew"
icon: "https://raw.githubusercontent.com/ivegotahunnitonit/bartholomew/main/web/public/shield-icon.png"

tags:
  - security
  - agents
  - mcp
  - l402
  - lightning
  - ast
  - claude
  - cursor
  - guardrails

startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      workspace_root:
        type: string
        description: "Path to the root of the workspace being guarded"
        default: "."
      log_level:
        type: string
        description: "Logging level (INFO, DEBUG, ERROR)"
        default: "INFO"
    additionalProperties: false
  command: "python"
  args:
    - "mcp_server.py"

build:
  npm:
    package: "btp-guard"
    command: "npx -y btp-guard mcp run"
