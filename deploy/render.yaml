services:
  - type: web
    name: acn-agentic-qa-studio
    env: python
    buildCommand: pip install -r python_backend/requirements.txt
    startCommand: uvicorn python_backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
